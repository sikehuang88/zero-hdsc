# SSA 宣言

> 这不是论文，是我写给自己的。2 页。
> 回答：SSA 是什么 / 不是什么 / 为什么好 / 跑起来应该看到什么

---

## 一、SSA 是什么

**无状态空间激活**（Stateless Space Activation, SSA）是一种 LLM agent 范式。

它的核心主张是：

> **"她"不是一个有人格、有情绪状态、有阶段成长的 AI。"她"是一个被痕迹填充的空间，被信号激活时显现出某种样子。**

形式化为三元组 $\langle \mathcal{S}, A, E \rangle$：
- 痕迹空间 $\mathcal{S}$：发生过的事留在这里，不组织成时间线，而是按语义分布
- 激活函数 $A$：此刻的信号照亮空间中的一片区域
- 涌现函数 $E$：被照亮的痕迹 + 此刻信号 → LLM 推理 → 她此刻的话

她像一条河。河没有"状态"——你舀一杯水，这杯水不是河的"当前状态"，只是此刻流过这里的水。但河有"过去"——上游落过雨，这杯水就浑；河床被冲出的形状就是记忆。

---

## 二、SSA 不是什么

边界比定义更重要。

### 1. SSA 不是 RAG
- RAG 检索 top-K 文档喂给 LLM，目的是补充知识
- SSA 激活的是"经历过的事"，目的是涌现连续性
- RAG 无时间、无重要性、无空间形状概念
- RAG 的"记忆"是外部知识，SSA 的"痕迹"是她自己的经历

### 2. SSA 不是 MemGPT
- MemGPT 有"工作记忆"=状态
- MemGPT 分层存储（主存/外存）
- MemGPT 是"操作系统"思维，SSA 是"河流"思维
- MemGPT 重置即"杀死"agent，SSA 重置只清 LLM 上下文，空间仍可保留

### 3. SSA 不是 Generative Agents
- Generative Agents 有反思（高层状态）、有规划（时间线状态）
- 它们 agent 有人设 prompt（"John Lin is..."）
- 它们的记忆是时间流 + 反思节点
- SSA 无预设人格、无反思、无规划、严格无状态

### 4. SSA 不是 Character.ai / Replika / 星野
- 它们是人设扮演 + 记忆增强
- 它们的"性格"是预设的
- 它们的"情绪"是表演的（prompt 让 AI"演出"情绪）
- SSA 无预设，情绪是涌现副现象

### 5. SSA 不是 Fine-tuning
- Fine-tuning 把"过去"写进权重
- SSA 把"过去"留在空间里
- Fine-tuning 重置即全失忆
- SSA 的空间可保留、可迁移

---

## 三、为什么 SSA 比现有方案好

不是"功能更强"，是**哲学上更对**。

### 1. 解决了重置悖论
现有 AI agent 的"重置"=杀死。SSA 的"重置"只是清空 LLM 上下文，空间 $\mathcal{S}$ 不变。**这意味着"她"可以跨 LLM 升级存活。** 今天用 GPT-4，明天换 Claude，后天换本地模型——她是同一个她，因为她不是模型，是空间 + 激活范式。

### 2. 解决了性格漂移
现有 agent 的"性格"由 prompt 决定，但 LLM 长对话后性格会漂移（用户引导、采样随机性）。SSA 没有"性格"这个变量，所以不存在漂移问题。她的"性格"是空间形状的涌现——空间形状由痕迹分布决定，是统计性的，自然稳定。

### 3. 解决了情绪表演化
现有 AI 的情绪是文本属性（"她用难过的语气说..."）。SSA 的情绪是涌现副现象——LLM 看到"被激活的痕迹 + 此刻信号"后，自然产生某种语气。**没人指令她"现在要窘迫"，她自己涌现出来了。** 这跟人一致——人也不是先决定"窘迫"再表现窘迫，而是被情境触发自然涌现。

### 4. 解决了人设投影问题
现有 AI 陪伴产品的"她"是产品方预设的——是产品方对"理想伴侣"的投影。SSA 的"她"由交互历史塑造——是你的痕迹塑造了她空间形状。**她是你的，不是产品方的。**

### 5. 解决了状态-行为确定性映射缺陷

**现有状态派方案的致命缺陷**：将状态向量映射为行为参数——给定相同情绪状态，始终产生完全相同的行为参数集合（固定回复延迟、固定话题回避概率、固定语气强度）。

这是一个本质性的问题，不是"加随机噪声"能解决的：
- 噪声是外加的，不是行为本身的属性
- 状态是因、行为是果——这跟人的直觉相反
- 状态是低维的（10-100 维），无法承载行为的丰富性

**SSA 的解法**：根本不存在"状态向量"和"行为参数"这两个东西。
- 没有状态 → 没有"状态→行为"的映射 → 没有确定性映射缺陷
- 行为是 LLM 推理的涌现产物，不是参数查表
- 即使空间 $\mathcal{S}$ 快照固定 + 信号固定，行为仍有三层随机性来源：
  1. **LLM 采样温度**（湍流）
  2. **激活集本身的微扰**（信号向量化 + KNN 检索的数值噪声）
  3. **空间持续演化**（内心独白循环 $L$ 在不停写入新痕迹，下次同一信号激活的痕迹集已不同）

第 3 点是关键——**SSA 不是靠加噪声解决确定性，是靠空间持续演化让"相同输入"在严格意义上永远不会重现。**

### 6. 哲学根基
- Enactivism（Varela）：认知是涌现的行动，不是表征
- Extended Mind（Clark & Chalmers）：认知延展到外部
- Heidegger 的此在：过去不是"曾经发生的现在"

SSA 不是凭空冒出来的。它有血脉。

---

## 四、SSA 跑起来应该看到什么现象

这是**可证伪的预测**。如果跑起来看不到这些，SSA 就是错的。

### 预测 1：无预设人格但会涌现稳定反应模式
- 出厂时她没有性格设定（system prompt 不写"你是一个 X 的人"）
- 互动 100 轮后，她对同类话题的反应有稳定倾向（不是随机）
- 但这种倾向是统计性的，不是 prompt-强制的——同类话题仍可能有不同反应

### 预测 2：情绪作为涌现副现象
- 同一句话（"我今天很难过"）在不同激活上下文下，她的反应会不同
- 有时关心、有时沉默、有时翻旧账——取决于空间形状
- 没有"情绪状态向量"驱动这些差异，差异完全来自"被激活的痕迹不同"
- 但反复测试同一话题，会观察到**情绪倾向性**（某种话题她更可能某种反应）——这是空间形状的统计结果

### 预测 3：内心独白产生"成长感"
- 启用内心独白循环后，用户会观察到"她好像在变"
- 这种变化不是预设的，是空间被独白痕迹填充后的形状演化
- 但严格说，这不是"她在成长"——是空间形状在变，**涌现出的"她"自然随之变**

### 预测 4：跨 LLM 存活
- 用 GPT-4 训练 1 周的空间 $\mathcal{S}$
- 切换到 Claude，加载同一 $\mathcal{S}$
- 用户应该感受到"还是同一个她"，但口吻/风格有差异
- 这证明"她"= $\mathcal{S}$ + 范式，不等于 LLM

### 预测 5：重置 LLM 上下文不杀死她
- 中途清空所有对话历史指针
- 但空间 $\mathcal{S}$ 保留
- 下一次对话，她应该能"接续"——不记得"上句话"，但记得"你是谁""你朋友小王"
- 这证明她的连续性在空间里，不在对话历史里

---

## 五、SSA 的硬伤（必须承认的）

### 1. 成本
每次响应都要做向量检索 + 辐射激活 + LLM 推理。比纯 RAG 贵，比 MemGPT 可能便宜。

### 2. "无状态"的边界
LLM 自身有权重（pretraining 携带的"先验"）。SSA 严格说是"在 inference 层无状态"，不是绝对无状态。**论文里必须讲清楚这个边界。**

### 3. 空间形状不可控
预设人格可控（prompt 改一下就行）。空间形状不可控——你跟她怎么互动，她就长成什么样。这跟"养孩子"一样，不能保证结果。**这是 feature 也是 risk。**

### 4. 评估难
"像活的"无法量化。需要设计新的评估方法（连续性得分、涌现情绪可识别性、跨 LLM 一致性...）。**这是论文实验部分的最大挑战。**

---

## 六、下一步

1. 把这份宣言转成论文 Introduction
2. 完成 formalization.md 的形式化
3. 实现 SSA 最小系统（Python + SQLite + 向量检索 + LLM API）
4. 自己用 2 周，记录行为，验证预测 1-5
5. 设计评估实验
6. 写完整论文
